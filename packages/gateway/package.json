{
  "name": "@clawguard/gateway",
  "version": "1.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src --format esm --no-dts",
    "dev": "tsup src --format esm --watch",
    "start": "node dist/index.js",
    "test": "vitest --run"
  },
  "dependencies": {
    "@core/clawguard": "workspace:*",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.5.0"
  }
}
